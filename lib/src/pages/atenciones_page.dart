import 'package:flutter/material.dart';

class AtencionesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Atenciones'))
    );
  }
}